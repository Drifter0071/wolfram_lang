-- $project_name — Server Script
-- Welcomes players using Logger from shared
-- Output: ServerScriptService/Server/Main

import "../shared/logger.shared" as Log

local Players = game:GetService("Players")

Players.PlayerAdded:Connect(function(player) {
    Log.Logger:announce(player.Name, "joined the server")
})

