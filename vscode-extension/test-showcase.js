const fs = require("fs"); const { parseSource } = require("./out/lsp/parser"); const code = fs.readFileSync("..\\examples\\PartSpawnerShowcase.client.wrm", "utf-8"); const result = parseSource(code); if (result.errors.length > 0) { console.log("PARSE ERRORS:", result.errors.length); result.errors.slice(0, 10).forEach(e => console.log("  -", e)); } else { console.log("PASS: 0 parse errors"); } console.log("symbols:", result.symbols.length); console.log("imports:", result.imports.length);
